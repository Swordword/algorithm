#include "common.h"
class Solution
{
public:
  vector<int> decode(vector<int> &encoded, int first)
  {
    vector<int> arr;
    arr.push_back(first);
    for (auto i : encoded)
    {
      
    }

    return encoded;
  }
};